import { ChangeDetectorRef, ElementRef } from '@angular/core';
import { WcBtnDirective } from './wc-btn.directive';

describe('WcBtnDirective', () => {
  it('should create an instance', () => {

  });
});
